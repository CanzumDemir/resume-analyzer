# Web framework / server
fastapi~=0.136.1
uvicorn~=0.46.0
python-multipart~=0.0.27   # required by FastAPI for File()/Form() parsing

# Database
sqlmodel~=0.0.38
sqlalchemy~=2.0.49         # imported directly (Column/JSON) in models/analysis_result.py
psycopg2-binary~=2.9.12    # PostgreSQL driver

# Validation
pydantic~=2.13.3
pydantic-core~=2.46.3      # used directly for partial JSON parsing while streaming
email-validator~=2.3.0     # required at runtime by pydantic's EmailStr

# AI
openai~=2.32.0

# Auth
PyJWT~=2.13.0
pwdlib[argon2]~=0.3.0

# PDF text extraction
tika~=3.1.0

# Config
python-dotenv~=1.2.2

# Tests
httpx2~=2.12.0
pytest~=9.0.2
